import { Building2, CheckCircle2, CircleX, Layers3, X } from 'lucide-react';
import { motion } from 'framer-motion';
import { resolveNodeDetails } from '../../utils/nodeDetails';
export default function NodePanel({ node, onClose }) { if (!node) return null; const details = resolveNodeDetails(node.data); return <motion.aside initial={{ x: 40, opacity: 0 }} animate={{ x: 0, opacity: 1 }} exit={{ x: 40, opacity: 0 }} className="absolute bottom-3 right-3 top-3 z-20 w-[min(360px,calc(100%-24px))] overflow-y-auto rounded-2xl border border-white/10 bg-[#0b0f18]/95 p-5 shadow-2xl backdrop-blur-xl"><div className="flex items-start justify-between"><div><p className="text-xs uppercase tracking-widest text-cyan-300">Node intelligence</p><h3 className="mt-2 text-xl font-semibold">{details.title}</h3></div><button onClick={onClose} className="rounded-lg p-1 text-slate-500 hover:bg-white/5 hover:text-white"><X size={18} /></button></div><p className="mt-5 text-sm leading-6 text-slate-400">{details.what}</p><Section icon={Layers3} title="Why it is used"><p>{details.why}</p></Section><Section icon={CheckCircle2} title="Advantages" tone="text-emerald-300"><List items={details.advantages} /></Section><Section icon={CircleX} title="Disadvantages" tone="text-red-300"><List items={details.disadvantages} /></Section><Section icon={Layers3} title="Alternatives"><div className="flex flex-wrap gap-2">{details.alternatives.map((item) => <span key={item} className="rounded-lg bg-white/5 px-2.5 py-1 text-xs text-slate-300">{item}</span>)}</div></Section><Section icon={Building2} title="Used in industry"><List items={details.companies} /></Section></motion.aside>; }
function Section({ icon: Icon, title, tone = 'text-violet-300', children }) { return <section className="mt-6"><h4 className={`flex items-center gap-2 text-xs font-semibold uppercase tracking-wider ${tone}`}><Icon size={14} />{title}</h4><div className="mt-2 text-sm leading-6 text-slate-400">{children}</div></section>; }
function List({ items }) { return <ul className="space-y-1">{items.map((item) => <li key={item}>• {item}</li>)}</ul>; }
