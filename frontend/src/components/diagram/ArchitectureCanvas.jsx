import { Background, Controls, MiniMap, ReactFlow, useEdgesState, useNodesState } from '@xyflow/react';
import { useEffect, useState } from 'react';
import ArchitectureNode from './ArchitectureNode';
import NodePanel from './NodePanel';
const nodeTypes = { architecture: ArchitectureNode };
export default function ArchitectureCanvas({ initialNodes = [], initialEdges = [], onChange, exportId = 'architecture-canvas-export' }) { const [nodes, setNodes, onNodesChange] = useNodesState(initialNodes); const [edges, setEdges, onEdgesChange] = useEdgesState(initialEdges); const [selected, setSelected] = useState(null); useEffect(() => { setNodes(initialNodes); setEdges(initialEdges); }, [initialNodes, initialEdges, setNodes, setEdges]); useEffect(() => { if (onChange) onChange(nodes); }, [nodes, onChange]); return <div id={exportId} className="relative h-[620px] overflow-hidden rounded-2xl border border-white/10 bg-[#070a11]"><ReactFlow nodes={nodes} edges={edges} onNodesChange={onNodesChange} onEdgesChange={onEdgesChange} onNodeClick={(_, node) => setSelected(node)} onPaneClick={() => setSelected(null)} nodeTypes={nodeTypes} fitView fitViewOptions={{ padding: .25 }} minZoom={.25} maxZoom={1.8} proOptions={{ hideAttribution: true }}><Background color="#263249" gap={24} size={1} /><Controls className="!border-white/10 !bg-slate-900 !shadow-xl [&>button]:!border-white/10 [&>button]:!bg-slate-900 [&>button]:!fill-slate-300" /><MiniMap pannable zoomable className="!border !border-white/10 !bg-slate-950" nodeColor="#22d3ee" maskColor="rgba(3,7,18,.75)" /></ReactFlow><NodePanel node={selected} onClose={() => setSelected(null)} /></div>; }
