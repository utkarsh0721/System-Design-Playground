import { Copy, MoreHorizontal, Star, Trash2 } from 'lucide-react';
import { Link } from 'react-router-dom';
export default function DesignCard({ design, onFavourite, onDuplicate, onDelete }) { return <article className="glass group relative rounded-2xl p-5 transition duration-300 hover:-translate-y-1 hover:border-cyan-300/20"><div className="flex items-start justify-between gap-3"><div className="flex flex-wrap gap-2">{(design.tags || []).slice(0, 2).map((tag) => <span key={tag} className="rounded-lg bg-violet-300/10 px-2.5 py-1 text-[10px] font-medium text-violet-200">{tag}</span>)}</div><button aria-label="Favourite" onClick={() => onFavourite?.(design)} className={design.isFavourite ? 'text-amber-300' : 'text-slate-600 hover:text-amber-300'}><Star size={18} fill={design.isFavourite ? 'currentColor' : 'none'} /></button></div><Link to={`/app/designs/${design._id}`} className="mt-7 block"><h3 className="font-semibold text-white transition group-hover:text-cyan-200">{design.name}</h3><p className="mt-2 line-clamp-2 text-xs leading-5 text-slate-500">{design.input?.expectedUsers || 'Custom scale'} · {design.input?.architectureType || 'Architecture'}</p><p className="mt-5 text-[11px] text-slate-600">Updated {new Date(design.updatedAt || Date.now()).toLocaleDateString()}</p></Link><div className="absolute bottom-3 right-3 flex translate-y-1 gap-1 opacity-0 transition group-hover:translate-y-0 group-hover:opacity-100"><button onClick={() => onDuplicate?.(design)} aria-label="Duplicate" className="rounded-lg bg-slate-900 p-2 text-slate-400 hover:text-white"><Copy size={14} /></button><button onClick={() => onDelete?.(design)} aria-label="Delete" className="rounded-lg bg-slate-900 p-2 text-slate-400 hover:text-red-300"><Trash2 size={14} /></button><span className="rounded-lg bg-slate-900 p-2 text-slate-600"><MoreHorizontal size={14} /></span></div></article>; }
