import { BookOpen, Boxes, GitCompareArrows, GraduationCap, LayoutDashboard, Menu, Plus, Settings, Star, UserRound, X } from 'lucide-react';
import { useState } from 'react';
import { NavLink } from 'react-router-dom';
import { NAV_ITEMS } from '../../utils/constants';
const icons = { BookOpen, Boxes, GitCompareArrows, GraduationCap, LayoutDashboard, Plus, Settings, Star, UserRound };
export default function MobileNav() { const [open, setOpen] = useState(false); const quick = [{ path: '/app', label: 'Home', icon: LayoutDashboard }, { path: '/app/new', label: 'Design', icon: Plus }, { path: '/app/learn', label: 'Learn', icon: BookOpen }]; return <><nav className="fixed inset-x-3 bottom-3 z-40 flex items-center justify-around rounded-2xl border border-white/10 bg-slate-950/90 px-2 py-2 shadow-2xl backdrop-blur-xl lg:hidden">{quick.map(({ path, label, icon: Icon }) => <NavLink key={path} to={path} end={path === '/app'} className={({ isActive }) => `flex min-w-16 flex-col items-center gap-1 rounded-xl py-1.5 text-[10px] ${isActive ? 'text-cyan-300' : 'text-slate-500'}`}><Icon size={19} />{label}</NavLink>)}<button onClick={() => setOpen(true)} className="flex min-w-16 flex-col items-center gap-1 rounded-xl py-1.5 text-[10px] text-slate-500"><Menu size={19} />More</button></nav>{open && <div className="fixed inset-0 z-50 bg-black/70 backdrop-blur-sm lg:hidden" onClick={() => setOpen(false)}><div className="absolute bottom-0 w-full rounded-t-3xl border-t border-white/10 bg-[#0c1019] p-5" onClick={(event) => event.stopPropagation()}><div className="flex items-center justify-between"><p className="font-semibold">Navigate</p><button onClick={() => setOpen(false)}><X /></button></div><div className="mt-5 grid grid-cols-2 gap-2">{NAV_ITEMS.map((item) => { const Icon = icons[item.icon]; return <NavLink onClick={() => setOpen(false)} key={item.path} to={item.path} className="flex items-center gap-3 rounded-xl bg-white/5 p-3 text-sm text-slate-300"><Icon size={17} />{item.label}</NavLink>; })}</div></div></div>}</>; }
