export const buildLearningPrompt = (topic) => `Act as a senior distributed-systems educator. Create an accurate, practical lesson about ${topic}. Return only JSON with: title, summary, explanation, mentalModel, realWorldExamples (array), advantages (array), disadvantages (array), interviewQuestions (array of {question,answer}), animationSteps (array of {label,description}), and relatedTopics (array). Avoid marketing claims.`;
